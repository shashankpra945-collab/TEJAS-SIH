import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arogyamitra.app',
  appName: 'ArogyaMitra',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
